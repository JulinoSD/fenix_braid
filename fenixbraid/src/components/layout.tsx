import { FunctionComponent, ReactNode } from "react"

interface IProps {
    main: ReactNode
}
export const Layout: FunctionComponent<IProps> = ({main}) => {
    return (
        <div>
            
        </div>
    )
}